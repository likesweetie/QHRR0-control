# QHRR0 Control 리팩토링 지침

이 문서는 이 프로젝트를 리팩토링할 때 Codex가 계속 참조할 작업 기준이다.
목표는 레거시 구조를 제거하고, 런타임 흐름과 설정의 단일 기준 소스를 분명하게 만드는 것이다.

## 리팩토링 원칙

- 단일 기준 소스(single source of trust)를 먼저 정한다. 같은 의미의 설정, 경로, 타입, 상태값이 여러 파일에 흩어져 있으면 하나만 남긴다.
- silent fallback 금지. 누락된 설정, 알 수 없는 드라이버, 잘못된 import 경로, 깨진 YAML은 기본값으로 덮지 말고 명시적으로 실패시킨다.
- 지나친 추상화 금지. 하드웨어 제어 흐름은 가능한 한 한 파일 또는 한 계층에서 순서가 눈에 보이게 유지한다.
- 생산자가 데이터를 검증한다. SHM 소비자는 크리티컬한 예외를 제외하면 데이터를 보정하거나 추측하지 않는다.
- 런타임 정책은 해당 프로세스가 소유한다. validation policy, fallback policy 같은 런타임 판단은 config에 숨기지 말고 소유 프로세스의 명시적 코드로 둔다.
- 레거시 호환 alias는 원칙적으로 만들지 않는다. 정말 필요한 경우에도 제거 예정 날짜와 호출부 전환 계획을 같이 둔다.
- `third_party/`는 기본적으로 수정 대상이 아니다. 단, 프로젝트가 직접 붙인 MuJoCo CAN bridge나 펌웨어 호환 코드는 별도 근거가 있을 때만 수정한다.

## 현재 구조 요약

### 제어기 런타임

- 주 진입점 후보: `qhrr0/app/robot_controller/main.py`
- 핵심 런타임 객체: `qhrr0/app/robot_controller/controller.py`의 `RobotController`
- 현재 의도된 시작 순서:
  1. SHM stale cleanup 및 segment 생성
  2. `can_daemon` subprocess 시작
  3. CAN transport 연결 및 callback 등록
  4. IMU startup request
  5. 나머지 subprocess 시작
  6. control loop 실행
- control loop의 안전상 중요한 분기:
  - `ESTOP`, `DISABLED`: actuator disable frame만 송신
  - `ENABLING`: enable frame만 송신
  - `ZERO_SETTING`: zero-set command 송신
  - `DAMPING`: damping MIT command 송신
  - `NORMAL`: `ControlCommandShm.read_relaxed()`를 읽고 MIT command 송신

### 하위 프로세스

- 관리 주체: `qhrr0/app/robot_controller/process_supervisor/process_supervisor.py`
- 설정 후보: `qhrr0/config/app_config/subprocesses.yaml`
- 현재 프로세스:
  - `can_daemon`
  - `aux_reader`
  - `task_controller`
  - `dashboard`
  - `system_logger`는 파일은 있으나 config 연결 여부 확인 필요

### CAN/HAL

- CAN bus 공통 계층: `qhrr0/app/hal/can_bus/`
- generic CAN hardware driver 계층: `qhrr0/app/hal/hardware/can/`
- 구체 장치 protocol/driver 계층: `qhrr0/app/hal/driver/`
- `dongilc_protocol.py`는 실제로 `SPGActuatorProtocol`을 담고 있어 이름과 책임이 어긋난다. 참조부 전환 후 `spg_protocol.py`로 정리하는 것이 우선 후보다.

### 공유메모리

- SHM manager: `qhrr0/app/robot_controller/shm/manager.py`
- ctypes layout: `qhrr0/app/robot_controller/shm/types/`
- `CStructShm` 책임:
  - shared memory create/open/close/unlink
  - segment size 검증
  - ctypes 구조체와 bytes 변환
  - buffer backend read/write/clear 위임
- `CStructShm` 비책임:
  - command 생성
  - timestamp 생성
  - 런타임 데이터 의미 검증

### 설정

- 경로 registry 후보: `qhrr0/config/config_paths.yaml`
- app config 후보:
  - `qhrr0/config/app_config/robot_controller_config.yaml`
  - `qhrr0/config/app_config/subprocesses.yaml`
  - `qhrr0/config/app_config/sub_process/can_daemon.yaml`
  - `qhrr0/config/app_config/sub_process/dashboard.yaml`
- robot config 후보:
  - `qhrr0/config/qhrr0_description.yaml`
  - `qhrr0/config/robot_config/can_device_config.yaml`
- loader/validator: `qhrr0/app/helper/config_manage/`

## 먼저 고쳐야 하는 구조 문제

1. import 경로를 하나로 통일한다.
   - 테스트는 `qhrr0.app...` 경로를 사용한다.
   - 현재 코드에는 `robot.*`, `robot_controller.*`, `hal.*`, `helper.*`, `hw_driver.*`가 섞여 있다.
   - 새 기준은 `qhrr0.app...` 절대 import로 잡고, 호출부를 직접 수정한다.

2. 설정 계약을 복구한다.
   - `config_paths.yaml`는 현재 `subprocesses` 값의 따옴표가 닫히지 않아 YAML로 읽을 수 없다.
   - `robot_controller_config.yaml`는 validator가 기대하는 `robot_controller.control_hz`, `can.daemon`, `can.mit_protocol_range` 등의 구조와 맞지 않는다.
   - `can_daemon` 테스트는 `load_can_daemon_config(controller_config, daemon_config)`를 기대하지만 현재 구현 파일에 해당 함수가 없다.
   - 설정은 조용히 합치지 말고, 어떤 파일의 어떤 section이 최종 config에 들어가는지 loader에서 명시해야 한다.

3. `RobotController` 생성 흐름을 정상화한다.
   - `RobotController.__init__`는 `config: dict`를 요구하지만 `main.py`는 `RobotController()`로 호출한다.
   - `main.py`에는 삭제되었거나 존재하지 않는 factory/config/spec import가 남아 있다.
   - 먼저 config load -> validate -> `RobotController(config)` 흐름을 한 곳에서 보이게 만든다.

4. SHM 타입 import를 정리한다.
   - 현재 일부 하위 프로세스는 `robot_controller.shm.types.aux_command`, `control_command`, `operator_command` 같은 과거 파일명을 import한다.
   - 현재 실제 파일은 `types/commands.py`, `types/robot_state.py`다.
   - compatibility module을 추가하기보다 호출부를 실제 파일 구조로 수정한다.

5. actuator output source 이름을 맞춘다.
   - `RobotController._send_mit_command()`는 `"MIT"`를 기록한다.
   - `COMMAND_OUTPUT_SOURCE_VALUES`에는 `"POLICY"`는 있지만 `"MIT"`가 없다.
   - 현재는 source가 `0`으로 떨어질 수 있으므로, 안전 모니터링 전에 enum 이름을 확정해야 한다.

6. 상태 머신 주석을 정리한다.
   - `state_machine.py` 하단에는 설계 설명이 코드보다 길게 붙어 있다.
   - 핵심 계약은 이 문서나 별도 design doc으로 옮기고, 코드에는 분기 의도를 이해하는 데 필요한 짧은 주석만 남긴다.

## 제거 또는 정리 후보

- `qhrr0/qhrr0.py`: 현재 `pass`만 있는 빈 entrypoint다. 실제 사용처가 없으면 제거한다.
- `README.md`의 `robot_controller.main`, `config/app_config/robot_controller.yaml` 경로: 현재 파일 구조와 맞지 않는다. 실행 가능 경로가 복구된 뒤 갱신한다.
- `run_mujoco_simulation.py`: `robot_controller.config`, `robot_controller.platform.config` 등 레거시 모듈에 의존한다. MuJoCo 실행을 유지할지 결정한 뒤 현대화하거나 격리한다.
- `dongilc_protocol.py`: SPG 프로토콜 구현 파일로 이름을 바꾼다. `shortest_delta_u14()`는 현재 TODO에 제거 검토 대상으로 적혀 있고 직접 참조가 보이지 않으므로 테스트를 붙인 뒤 유지 여부를 결정한다.
- `valid_targets()`, `valid_actuators()` 류의 소비자 보정 helper: 생산자 검증 원칙과 충돌할 수 있다. dashboard 표시용 편의인지, 런타임 제어용인지 구분해서 줄인다.
- 삭제된 과거 설정 파일명:
  - `config/app_config/robot_controller.yaml`
  - `config/app_config/processes.yaml`
  - `config/app_config/dashboard.yaml`
  - 새 이름과 registry 기준으로 README, subprocess command, 테스트를 맞춘다.

## 즉시 삭제하면 안 되는 것

- `third_party/mujoco`, `third_party/onnxruntime`: 용량이 크지만 외부 런타임 의존성이다. 제거는 별도 배포 전략이 있을 때만 한다.
- `tools/can_monitor.py`: dashboard와 기능이 겹쳐 보여도 독립 진단 도구일 수 있다. 실제 사용 여부를 확인하기 전 삭제하지 않는다.
- dashboard backend/frontend: 구조는 크지만 operator command와 SHM 상태 확인 경로를 제공한다. controller 안전 흐름이 안정된 뒤 정리한다.
- C++ MuJoCo CAN bridge: simulation CAN loop에 연결될 수 있으므로 Python import 검색만으로 삭제 판단하지 않는다.

## 작업 순서

1. 현재 테스트 기준을 확인한다.
   - 최소 기준: `pytest tests/test_config_loader_base.py tests/test_config_validator.py tests/test_shm_manager.py tests/test_process_supervisor.py`
   - CAN daemon config 계약을 구현한 뒤 `tests/test_can_daemon_config.py`를 포함한다.

2. 패키지/import 기준을 고정한다.
   - `qhrr0`를 최상위 패키지로 사용한다.
   - `robot_controller`, `hal`, `helper`, `robot`, `hw_driver` import를 단계적으로 제거한다.
   - import fallback branch는 새로 만들지 않는다.

3. config registry와 loader를 정리한다.
   - `config_paths.yaml` 문법을 먼저 고친다.
   - 최종 `RobotController` config를 만드는 compose 함수 하나를 둔다.
   - compose 함수는 `robot_platform`, `can_device`, `robot_controller`, `subprocesses`, `can_daemon` 입력을 명시적으로 읽는다.

4. `main.py`와 subprocess entrypoint를 실행 가능하게 만든다.
   - `python3 -m qhrr0.app.robot_controller.main`
   - `python3 -m qhrr0.app.robot_controller.subprocesses.can_daemon.main`
   - README의 실행 명령도 이 기준으로 맞춘다.

5. controller 안전 흐름을 깨지 않는 범위에서 내부 가독성을 개선한다.
   - mode별 actuator output path는 한눈에 보여야 한다.
   - enable/damping/zero/policy 송신 loop는 과도하게 추상화하지 않는다.
   - safety 관련 이름은 실제 frame 의미와 일치시킨다.

6. dashboard/task/aux reader를 새 SHM/config/import 기준으로 맞춘다.
   - 오래된 파일명 import를 실제 `types/commands.py`, `types/robot_state.py` 기준으로 변경한다.
   - 소비자에서 데이터 보정이 필요한 경우 화면 표시용인지 제어용인지 분리한다.

7. 레거시 파일 제거는 마지막에 한다.
   - `rg`로 참조가 없는지 확인한다.
   - 제거 전후 테스트 결과를 비교한다.
   - README/TODO/문서의 경로도 함께 갱신한다.

## 리팩토링 중 확인할 명령

```bash
rg -n "from robot\\.|from robot_controller|from hal|from helper|hw_driver" qhrr0 tests run_mujoco_simulation.py tools
rg -n "robot_controller\\.yaml|dashboard\\.yaml|processes\\.yaml|config_paths" README.md qhrr0 tests
pytest tests/test_config_loader_base.py tests/test_config_validator.py tests/test_shm_manager.py tests/test_process_supervisor.py
pytest tests/test_can_daemon_config.py
```

## 완료 기준

- controller와 subprocess entrypoint가 `qhrr0.app...` 기준으로 실행된다.
- config registry 한 곳에서 최종 설정 파일 경로를 추적할 수 있다.
- 잘못된 설정은 기본값으로 보정되지 않고 예외로 실패한다.
- SHM layout과 producer/consumer 책임이 문서와 코드에서 일치한다.
- 안전 관련 mode별 actuator output path가 코드에서 직접 읽힌다.
- 레거시 import, 삭제된 config 파일명, 빈 entrypoint가 남지 않는다.
