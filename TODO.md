사람 용이니까 agent나 언어모델들은 이거 읽지 맛

@ donginC_protocol.py
 shortest_delta_u14 <- 이거 필요?

각 서브 모듈들 기본값 검토하고 날리기

spg_firmware.cc <-- 여기에 있는 uint_to_float도 수정

만약 MIT 명령이 0이 아니라면 눈에 잘띄게 표시하기

validation policy 클래스를 만들어서 필요한 각 프로세스들이 소유하도록 하고, fallback 정책 등을 여기가 런타임에서 관리 <- config로 관리 아님

데이터 검증은 기본적으로 생산자 책임
정말 크리티컬한 데이터가 아니라면 소비자는 데이터를 믿고 사용하며
소비자는 데이터 자체를 가공해서는 안된다 (SPMC 구조를 간접적으로 강제)
런타임 에러가 발생할 위험이 있더라도 리딩 헬퍼 코드는 사용하지 마